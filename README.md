# Un-definer

Primitive Chrome/Chromium extension for Codeforces pages.

It adds a red minus button near every `#define` and an orange minus button near every simple `typedef` found inside a code block. Clicking a button turns it into a plus, strikes through the declaration line, and rewrites matching usages in the shown code.
